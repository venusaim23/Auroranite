<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hack.auroranite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_auroranite_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_auroranite_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Auroranite">
        <activity
            android:name=".Activities.UpdateUser"
            android:exported="false" />
        <activity
            android:name=".Activities.HomeScreen"
            android:exported="false"
            android:theme="@style/Theme.Auroranite.NoActionBar" />
        <activity
            android:name=".Activities.CreateAccount"
            android:exported="false"
            android:label="@string/create_account_" />
        <activity
            android:name=".Activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Auroranite.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>